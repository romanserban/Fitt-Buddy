<% layout('layout/page')%>
    <div class="container d-flex justify-content-center align-items-center mt-5">
        <ul>

            <% for (let m of user.meal) { %>
                <li>
                    <div class="card m-2">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="card-body">
                                    <h3 class="card-title" style=" text-transform: capitalize;">
                                        <%= m.name %>
                                    </h3>
                                    <hr>
                                    <div class="d-flex">
                                        <p class="card-text m-2">
                                            Calories: <%= m.calories %>
                                        </p>
                                        <p class="card-text m-2">
                                            Carbs: <%= m.carbs %>
                                        </p>
                                        <p class="card-text m-2">
                                            Fats: <%= m.fats %>
                                        </p>
                                        <p class="card-text m-2">
                                            Proteins: <%= m.protein %>
                                        </p>
                                    </div>

                                    <div class="d-flex justify-content-end">
                                        <form action="/meal/<%= m._id %>/add" method="POST">
                                            <button class="btn btn-primary m-2">Add meal</button>
                                        </form>
                                        <form action="/meal/<%= m._id %>/add" method="POST">
                                            <button class="btn btn-primary m-2">Add meal</button>
                                        </form>
                                        <form action="/meal/<%= m._id %>/add" method="POST">
                                            <button class="btn btn-primary m-2">Add meal</button>
                                        </form>

                                    </div>

                                </div>
                            </div>
                        </div>

                        <% } %>
                    </div>
                </li>



        </ul>
    </div>